# Автоматическая установка нод Renga для Sverchok

## 🚀 Быстрая установка

### Способ 1: Автоматический (рекомендуется)

1. **Скачайте проект** или скопируйте папку `sverchok_nodes`

2. **Запустите установку**:
   - **Windows**: Двойной клик на `install.bat`
   - **Или вручную**: Откройте Blender → Text Editor → скопируйте `install_renga_nodes.py` → Run Script

3. **Перезапустите Blender** полностью

4. **Готово!** Ноды появятся в категории "Renga" в меню Sverchok

### Способ 2: Ручной (если автоматический не работает)

1. Скопируйте папку `sverchok_nodes` из проекта
2. Переименуйте её в `renga`
3. Скопируйте в:
   ```
   %APPDATA%\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
   ```
4. Добавьте категорию в меню (см. `sverchok_nodes/MENU_REGISTRATION.md`)
5. Перезапустите Blender

## 📋 Что делает скрипт установки

1. ✅ Находит папку Sverchok автоматически
2. ✅ Копирует все файлы нод в правильное место
3. ✅ Добавляет категорию "Renga" в меню YAML файлы
4. ✅ Удаляет старый кэш Python
5. ✅ Готово к использованию!

## 🔧 Установка на другой компьютер

### Для Renga (плагин C#):

1. Соберите проект в Visual Studio
2. Скопируйте `RengaGH.dll` и `RengaGH.rndesc` в папку плагинов Renga
3. Перезапустите Renga

### Для Blender (ноды Sverchok):

1. Скопируйте папку `sverchok_nodes` на новый компьютер
2. Запустите `install.bat` или `install_renga_nodes.py` в Blender
3. Готово!

## ⚠️ Требования

- **Blender** 5.0 или 4.x
- **Sverchok** аддон установлен
- **Renga** с плагином RengaGH (для работы нод)

## 🐛 Решение проблем

Если ноды не появляются:

1. Проверьте, что папка `renga` находится в:
   ```
   %APPDATA%\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
   ```

2. Проверьте, что категория "Renga" добавлена в файлы меню:
   ```
   %APPDATA%\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\menus\index.yaml
   ```

3. Удалите `__pycache__` в папках `nodes/renga/` и `menus/`

4. Полностью перезапустите Blender

5. Проверьте регистрацию нод в консоли Blender:
   ```python
   import bpy
   print(hasattr(bpy.types, 'SvRengaConnectNode'))
   ```

## 📝 Структура после установки

```
sverchok-master/
  nodes/
    renga/                    ← ВОТ СЮДА
      __init__.py
      renga_connect.py
      renga_create_columns.py
      renga_get_walls.py
      renga_client.py
      commands.py
      connection_protocol.py
  menus/
    index.yaml                ← Категория "Renga" добавлена
    full_by_data_type.yaml    ← Категория "Renga" добавлена
    full_by_operations.yaml   ← Категория "Renga" добавлена
```

## ✅ Проверка установки

После установки:

1. Откройте Blender
2. Откройте Sverchok
3. Нажмите **Add Node** или **Space**
4. Найдите категорию **"Renga"** в меню
5. Должны быть видны 3 ноды:
   - Renga Connect
   - Renga Create Columns
   - Renga Get Walls

Если всё видно - установка успешна! 🎉

