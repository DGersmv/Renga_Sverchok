# ФИНАЛЬНАЯ ИНСТРУКЦИЯ: Установка и регистрация нод Renga

## На основе всех инструкций в проекте

### ✅ ЧТО УЖЕ СДЕЛАНО

1. **`__init__.py`** - содержит функции `register()` и `unregister()` с правильными импортами
2. **Все ноды имеют `sv_category = "Renga"`** - для появления в меню
3. **Все ноды имеют `sv_icon`** - для иконок в меню
4. **Убраны относительные импорты** - используются прямые импорты или `importlib.util`

---

## 📋 ПОШАГОВАЯ УСТАНОВКА

### ШАГ 1: Копирование файлов

Скопируйте **ВСЕ файлы** из:
```
C:\Program Files\Renga Standard\RengaSDK\Samples\C#\Renga_Sverchok\sverchok_nodes\renga\
```

В папку Sverchok:
```
C:\Users\<ВашеИмя>\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
```

**Структура должна быть:**
```
nodes/renga/
├── __init__.py              ← ОБЯЗАТЕЛЬНО (с register/unregister)
├── renga_connect.py         ← с sv_category = "Renga"
├── renga_create_columns.py  ← с sv_category = "Renga"
├── renga_get_walls.py      ← с sv_category = "Renga"
├── renga_client.py
├── commands.py
└── connection_protocol.py
```

### ШАГ 2: Удаление кэша Python

1. **Полностью закройте Blender**
2. Удалите папку `__pycache__` в `nodes\renga\` (если есть)
3. Удалите все файлы `.pyc` (если есть)

### ШАГ 3: Запуск Blender

1. Запустите Blender заново
2. Убедитесь, что аддон Sverchok активирован

### ШАГ 4: Проверка автоматической регистрации

Откройте консоль Blender (`Window > Toggle System Console`) и проверьте наличие сообщений:
```
Renga nodes: Registered renga_connect
Renga nodes: Registered renga_create_columns
Renga nodes: Registered renga_get_walls
```

Если сообщений нет - переходите к ШАГУ 5.

### ШАГ 5: Явная регистрация (если автоматическая не сработала)

Используйте один из скриптов регистрации:

#### Вариант A: `register_renga_fixed.py` (рекомендуется)

1. Откройте файл `register_renga_fixed.py` в Text Editor Blender
2. Обновите путь в строке 15 (если нужно):
   ```python
   renga_path = r"C:\Users\<ВашеИмя>\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga"
   ```
3. Нажмите **Run Script** (Alt+P)
4. Проверьте вывод в консоли

#### Вариант B: `register_renga_final.py`

Аналогично варианту A, но с другим способом импорта.

#### Вариант C: Прямой код из FINAL_SOLUTION.md

Выполните в консоли Python Blender:

```python
import bpy
import sys
import os

# Путь к нодам
renga_path = r"C:\Users\<ВашеИмя>\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga"

# Добавить путь в sys.path
sverchok_nodes_path = os.path.dirname(renga_path)
if sverchok_nodes_path not in sys.path:
    sys.path.insert(0, sverchok_nodes_path)

# Импорт и регистрация
try:
    import nodes.renga.renga_connect as rc
    import nodes.renga.renga_create_columns as rcc
    import nodes.renga.renga_get_walls as rgw
    
    rc.register()
    rcc.register()
    rgw.register()
    
    print("✓ Ноды зарегистрированы!")
    
    # Проверка
    print("Проверка:")
    print(f"  SvRengaConnectNode: {'SvRengaConnectNode' in dir(bpy.types)}")
    print(f"  SvRengaCreateColumnsNode: {'SvRengaCreateColumnsNode' in dir(bpy.types)}")
    print(f"  SvRengaGetWallsNode: {'SvRengaGetWallsNode' in dir(bpy.types)}")
    
except Exception as e:
    print(f"Ошибка: {e}")
    import traceback
    traceback.print_exc()
```

### ШАГ 6: Проверка регистрации

Выполните в консоли Python Blender:

```python
import bpy

print("Проверка нод:")
print("SvRengaConnectNode:", 'SvRengaConnectNode' in dir(bpy.types))
print("SvRengaCreateColumnsNode:", 'SvRengaCreateColumnsNode' in dir(bpy.types))
print("SvRengaGetWallsNode:", 'SvRengaGetWallsNode' in dir(bpy.types))
```

**Если все три команды возвращают `True` - ноды зарегистрированы!**

### ШАГ 7: Поиск нод в меню Sverchok

#### Способ 1: Через категорию (если работает)

1. Откройте Sverchok
2. Нажмите `Add Node` (или `Space`)
3. Найдите категорию **"Renga"**
4. Должны быть видны 3 ноды

#### Способ 2: Через поиск (если категория не видна)

1. Откройте Sverchok
2. Нажмите **Space** (или кнопку поиска)
3. Введите **"Renga"** или **"SvRenga"**
4. Ноды должны появиться в результатах поиска

#### Способ 3: Программное добавление (если меню не работает)

```python
import bpy

# Создать дерево нод Sverchok
tree = bpy.context.space_data.edit_tree
if tree and tree.bl_idname == 'SverchCustomTreeType':
    # Добавить ноду через API
    node = tree.nodes.new('SvRengaConnectNode')
    node.location = (0, 0)
    print("✓ Нода добавлена вручную!")
```

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Проблема 1: Ноды регистрируются, но не видны в меню

**Причина:** Известная проблема Sverchok v1.4.0 с Blender 5.0

**Решение:**
- Используйте поиск (Space → "Renga")
- Или добавьте ноды программно (см. Способ 3 выше)

### Проблема 2: Ошибки импорта

**Причина:** Неправильный путь или отсутствие файлов

**Решение:**
1. Проверьте, что все файлы скопированы
2. Проверьте путь в скриптах регистрации
3. Убедитесь, что `__init__.py` присутствует

### Проблема 3: Ноды не регистрируются автоматически

**Причина:** Sverchok не вызывает `register()` из `__init__.py`

**Решение:**
- Используйте явную регистрацию (ШАГ 5)
- Перезагрузите аддон Sverchok:
  - `Edit > Preferences > Add-ons`
  - Найдите "Sverchok"
  - Снимите галочку (отключите)
  - Поставьте галочку обратно (включите)

---

## 🔍 ПРОВЕРКА ВСЕХ КОМПОНЕНТОВ

### 1. Проверка файлов

Убедитесь, что все файлы на месте:
- `__init__.py` - с функциями `register()` и `unregister()`
- `renga_connect.py` - с `sv_category = "Renga"`
- `renga_create_columns.py` - с `sv_category = "Renga"`
- `renga_get_walls.py` - с `sv_category = "Renga"`
- `renga_client.py`
- `commands.py`
- `connection_protocol.py`

### 2. Проверка регистрации

```python
import bpy
print('SvRengaConnectNode' in dir(bpy.types))
print('SvRengaCreateColumnsNode' in dir(bpy.types))
print('SvRengaGetWallsNode' in dir(bpy.types))
```

### 3. Проверка меню

- Попробуйте поиск (Space → "Renga")
- Попробуйте категорию "Renga" в меню
- Попробуйте программное добавление

---

## 📝 ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Все файлы скопированы в `nodes\renga\`
- [ ] `__init__.py` содержит `register()` и `unregister()`
- [ ] Все ноды имеют `sv_category = "Renga"`
- [ ] Кэш Python удален (`__pycache__`)
- [ ] Blender перезапущен
- [ ] Ноды зарегистрированы (проверка через `dir(bpy.types)`)
- [ ] Ноды найдены через поиск или категорию

---

## 🎯 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

1. **Проверьте консоль Blender** на ошибки
2. **Проверьте версию Sverchok** - обновите до последней
3. **Попробуйте с Blender 4.x** для проверки совместимости
4. **Используйте скрипт `simple_check.py`** для диагностики

---

## 📚 ИСПОЛЬЗОВАННЫЕ ИСТОЧНИКИ

- `FINAL_SOLUTION.md` - способ регистрации через `nodes.renga.module_name`
- `INSTALL_FINAL.md` - убраны относительные импорты
- `register_renga_fixed.py` - правильная настройка `sys.path` и `__package__`
- `TROUBLESHOOTING.md` - проверка меню и категорий
- `КАК_ПОЯВИТЬ_В_МЕНЮ.md` - использование поиска

