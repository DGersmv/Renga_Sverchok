# Инструкция по настройке проекта в Visual Studio

## Шаг 1: Открыть решение

1. Откройте файл `Renga_Grasshopper.sln` в Visual Studio

## Шаг 2: Восстановить NuGet пакеты

### Автоматическое восстановление:
- Visual Studio обычно автоматически предлагает восстановить пакеты при открытии решения
- Если появилось уведомление, нажмите **"Restore"** или **"Восстановить"**

### Ручное восстановление:
1. Правой кнопкой мыши на решении `Renga_Grasshopper` в Solution Explorer
2. Выберите **"Restore NuGet Packages"** или **"Восстановить пакеты NuGet"**

Или через меню:
- **Tools** → **NuGet Package Manager** → **Package Manager Console**
- Выполните команду: `dotnet restore`

## Шаг 3: Проверить зависимости проекта Renga

Проект **Renga** использует:
- ✅ **Newtonsoft.Json** (версия 13.0.3) - будет восстановлен автоматически через NuGet
- ✅ **Renga.NET8.PluginUtility.dll** - путь: `C:\Program Files\Renga Standard\RengaSDK\Net\Renga.NET8.PluginUtility.dll`
- ✅ **RengaCOMAPI.tlb** - путь: `C:\Program Files\Renga Standard\RengaSDK\tlb\RengaCOMAPI.tlb`

**Проверьте, что эти файлы существуют по указанным путям!**

## Шаг 4: Проверить зависимости проекта Grasshopper

Проект **Grasshopper** использует ссылки на DLL из Rhino 8:
- ✅ **Grasshopper.dll** - путь: `C:\Program Files\Rhino 8\Plug-ins\Grasshopper\Grasshopper.dll`
- ✅ **GH_IO.dll** - путь: `C:\Program Files\Rhino 8\Plug-ins\Grasshopper\GH_IO.dll`
- ✅ **RhinoCommon.dll** - путь: `C:\Program Files\Rhino 8\System\RhinoCommon.dll`
- ✅ **Newtonsoft.Json.dll** - путь: `C:\Program Files\Rhino 8\System\Newtonsoft.Json.dll`

**Если у вас Rhino установлен в другом месте, обновите пути в `Grasshopper.csproj`!**

## Шаг 5: Настроить конфигурацию сборки

1. В Visual Studio выберите конфигурацию: **Release** (или **Debug** для отладки)
2. Выберите платформу: **x64**
3. Убедитесь, что оба проекта выбраны для сборки:
   - Правой кнопкой на решении → **Properties** → **Configuration Properties**
   - Убедитесь, что для обоих проектов стоит галочка в колонке **Build**

## Шаг 6: Собрать решение

1. **Build** → **Build Solution** (или `Ctrl+Shift+B`)
2. Проверьте окно **Output** на наличие ошибок

### Ожидаемые результаты:

#### Проект GrasshopperRNG:
- ✅ Должен собраться успешно
- ✅ Результат: `GrasshopperRNG\bin\Release\Renga_Grasshopper.dll`
- ✅ Автоматически копируется в `%APPDATA%\Grasshopper\Libraries\Renga_Grasshopper.gha`

#### Проект RengaGH:
- ⚠️ Может быть предупреждение о TLB файле (MSB3287), но это не критично
- ✅ Должен собраться успешно, если Visual Studio правильно обработает COMFileReference
- ✅ Результат: `bin\Release\RengaGH\RengaGH.dll`
- ✅ Автоматически копируется в `C:\Program Files\Renga Standard\Plugins\RengaGH.dll` (вместе с зависимостями и иконкой `RengaGHIcon.png`)

## Шаг 7: Если возникают проблемы

### Проблема: "Не удается загрузить библиотеку типов для RengaCOMAPI.tlb"

**Решение 1:** Попробуйте собрать проект через Visual Studio (не через MSBuild в командной строке)

**Решение 2:** Если проблема сохраняется:
1. Убедитесь, что файл `RengaCOMAPI.tlb` существует по пути `C:\Program Files\Renga Standard\RengaSDK\tlb\RengaCOMAPI.tlb`
2. Попробуйте зарегистрировать TLB файл (запустите от имени администратора):
   ```
   regtlib "C:\Program Files\Renga Standard\RengaSDK\tlb\RengaCOMAPI.tlb"
   ```

### Проблема: "Не найдены DLL файлы Rhino"

**Решение:** Обновите пути в `Grasshopper.csproj` на актуальные пути к вашей установке Rhino 8

### Проблема: "NuGet пакеты не восстанавливаются"

**Решение:**
1. Проверьте подключение к интернету
2. Убедитесь, что в Visual Studio включен источник пакетов NuGet.org
3. Попробуйте вручную: **Tools** → **NuGet Package Manager** → **Package Manager Settings** → **Package Sources**

### Проблема: "Плагины не появляются после сборки"

**Решение для Grasshopper:**
1. **ВАЖНО: Удалите старый плагин перед установкой нового!**
   - Удалите файл `%APPDATA%\Grasshopper\Libraries\Renga_Grasshopper.gha` (если существует)
   - Это необходимо для избежания конфликтов GUID
2. Проверьте, что файл скопирован: `%APPDATA%\Grasshopper\Libraries\Renga_Grasshopper.gha`
3. Убедитесь, что файл имеет расширение `.gha` (не `.dll`)
4. Полностью закройте и перезапустите Rhino/Grasshopper
5. Проверьте в Grasshopper: **File** → **Special Folders** → **Libraries** - должна быть указана правильная папка
6. **Если ошибка "конфликт ID" или "конфликт GUID"**:
   - Удалите ВСЕ старые версии плагина из папки Libraries
   - Пересоберите проект (новые уникальные GUID уже установлены)
   - Убедитесь, что нет других плагинов с конфликтующими GUID
7. Компоненты должны появиться в категории **"RNG"**:
   - **"RNG"** - главная нода для подключения (GUID: 7A8B9C0D-1E2F-3456-789A-BCDEF0123456)
   - **"Input"** - нода для добавления колонн (GUID: 8B9C0D1E-2F34-5678-9ABC-DEF012345678)

**Решение для Renga:**
1. **Проверьте структуру папки:** Плагин должен быть в подпапке `C:\Program Files\Renga Standard\Plugins\RengaGH\`
2. Убедитесь, что скопированы все файлы в подпапку `RengaGH\`:
   - `RengaGH.dll` - основной файл плагина
   - `RengaGH.rndesc` - файл описания (имя должно совпадать с именем папки!)
   - `RengaGHIcon.png` - иконка плагина
   - `Interop.Renga.dll` - зависимости
   - `Renga.NET8.PluginUtility.dll` - зависимости
3. **Важно:** Файл `.rndesc` должен называться так же, как папка (например, `RengaGH\RengaGH.rndesc`)
4. **Важно:** Иконка `RengaGHIcon.png` должна быть в той же папке - без неё плагин может не отображаться
5. Если папка `Plugins` требует прав администратора, запустите Visual Studio от имени администратора и пересоберите проект
6. Полностью закройте и перезапустите Renga
7. Проверьте в Renga: **Настройки** → **Плагины** - должен быть виден плагин RengaGH с иконкой
8. Если плагин не появляется, проверьте лог: `%LOCALAPPDATA%\Renga Software\Renga\AecApp.log`

## Шаг 8: Проверка после сборки

После успешной сборки:

1. **Grasshopper плагин:**
   - Файл должен быть в: `%APPDATA%\Grasshopper\Libraries\Renga_Grasshopper.gha`
   - Перезапустите Rhino/Grasshopper, чтобы загрузить плагин

2. **Renga плагин:**
   - Файлы автоматически копируются в: `C:\Program Files\Renga Standard\Plugins\RengaGH\`
   - Структура папки:
     - `RengaGH.dll` - основной файл плагина
     - `RengaGH.rndesc` - файл описания плагина (должен совпадать с именем папки!)
     - `RengaGHIcon.png` - иконка плагина
     - `Interop.Renga.dll` - зависимости
     - `Renga.NET8.PluginUtility.dll` - зависимости
   - **Важно:** Плагин должен быть в подпапке `RengaGH`, а не прямо в `Plugins`
   - **Важно:** Имя файла `.rndesc` должно совпадать с именем папки
   - Перезапустите Renga, чтобы загрузить плагин

## Дополнительные настройки (опционально)

### Настройка путей к SDK (если нужно):

Если пути к Renga SDK или Rhino отличаются, обновите их в соответствующих `.csproj` файлах.

### Включение отладки:

Для отладки плагина Renga:
1. В свойствах проекта Renga → **Debug**
2. Установите **Start external program**: путь к `Renga.exe`
3. Установите **Working directory**: папка установки Renga

Для отладки плагина Grasshopper:
1. В свойствах проекта Grasshopper → **Debug**
2. Установите **Start external program**: путь к `Rhino.exe`
3. Установите **Working directory**: папка установки Rhino

---

**Готово!** Теперь вы можете собирать и отлаживать оба плагина в Visual Studio.

---

## 🔧 Если проект пропускается при сборке

Если Visual Studio показывает "Skipped Build" для проекта GrasshopperRNG:

1. **Используйте Rebuild Solution** вместо Build:
   - В меню: `Build` → `Rebuild Solution`
   - Или правый клик на Solution → `Rebuild`

2. **Очистите проект перед сборкой**:
   - Правый клик на проект `GrasshopperRNG` → `Clean`
   - Затем `Build` → `Build Solution`

3. **Проверьте, что файлы скопированы**:
   - После сборки проверьте наличие `Renga_Grasshopper.gha` в `%APPDATA%\Grasshopper\Libraries\`
   - Проверьте наличие `RengaGH.dll` в `C:\Program Files\Renga Standard\Plugins\RengaGH\`

---

## 🐛 Отладка проблем с отправкой данных

Если компонент "Input" показывает "Failed to send data to Renga or no response":

1. **Убедитесь, что сервер запущен в Renga**:
   - Откройте Renga
   - Нажмите кнопку "RNG" в панели инструментов
   - Нажмите "Start Server" (порт должен быть 50100)

2. **Проверьте подключение в Grasshopper**:
   - Компонент "RNG" должен показывать "Connected to Renga on port 50100"
   - Если нет - проверьте, что сервер запущен в Renga

3. **Проверьте правильность подключения**:
   - Выход "Client" (3-й выход) компонента "RNG" должен быть подключен ко входу "RengaConnect" компонента "Input"
   - НЕ подключайте выход "Connected" (boolean) - это только статус

4. **Используйте правый клик для отправки**:
   - Правый клик на компоненте "Input" → "Update - Send to Renga"
   - Кнопка "Update" на компоненте может не работать из-за ограничений API

5. **Проверьте отладочные сообщения**:
   - В Visual Studio: `View` → `Output` → выберите "Debug"
   - Ищите сообщения вида "Sending command to Renga" и "Received X bytes from server"


