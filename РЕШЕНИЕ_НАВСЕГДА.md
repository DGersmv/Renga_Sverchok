# 🎯 РЕШЕНИЕ ПРОБЛЕМЫ С ПОЯВЛЕНИЕМ НОД В МЕНЮ SVERCHOK - НАВСЕГДА

## ✅ ЧТО НУЖНО ДЛЯ РАБОТЫ:

### 1. Структура файлов (ОБЯЗАТЕЛЬНО):
```
nodes/renga/
├── __init__.py              ← ДОЛЖЕН содержать register() и unregister()
├── renga_connect.py         ← ДОЛЖЕН иметь sv_category = "Renga"
├── renga_create_columns.py  ← ДОЛЖЕН иметь sv_category = "Renga"
├── renga_get_walls.py       ← ДОЛЖЕН иметь sv_category = "Renga"
├── renga_client.py
├── commands.py
└── connection_protocol.py
```

### 2. Все ноды ДОЛЖНЫ иметь:
```python
class SvRengaXXXNode(SverchCustomTreeNode, bpy.types.Node):
    bl_idname = 'SvRengaXXXNode'
    bl_label = 'Renga XXX'
    sv_category = "Renga"  # ← ЭТО КРИТИЧНО!
```

### 3. Все файлы нод ДОЛЖНЫ иметь функции:
```python
def register():
    bpy.utils.register_class(SvRengaXXXNode)

def unregister():
    bpy.utils.unregister_class(SvRengaXXXNode)
```

---

## 🚀 ФИНАЛЬНОЕ РЕШЕНИЕ (ОДИН РАЗ):

### ШАГ 1: Регистрация нод

1. **Откройте Blender**
2. **Откройте Text Editor** (Shift+F11 или вкладка Scripting)
3. **Откройте файл** `FINAL_REGISTER_RENGA_NODES.py`
4. **Нажмите Run Script** (Alt+P или кнопка Run Script)
5. **Проверьте консоль** - должны быть сообщения:
   ```
   ✓ SvRengaConnectNode: ЗАРЕГИСТРИРОВАН
   ✓ SvRengaCreateColumnsNode: ЗАРЕГИСТРИРОВАН
   ✓ SvRengaGetWallsNode: ЗАРЕГИСТРИРОВАН
   ```

### ШАГ 2: Добавление в меню (КРИТИЧНО!)

**ВАЖНО:** Без этого шага ноды не появятся в меню!

#### Вариант A: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

1. **Откройте файл** `ADD_TO_YAML.py` в Text Editor Blender
2. **Нажмите Run Script** (Alt+P)
3. Скрипт автоматически добавит секцию Renga в меню

#### Вариант B: Ручное редактирование

1. **Откройте файл** в текстовом редакторе:
   ```
   C:\Users\ВАШЕ_ИМЯ\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\menus\full_by_data_type.yaml
   ```

2. **Найдите секцию** с "Network" и "Pulga Physics"

3. **Добавьте между ними:**
   ```yaml
   - Renga:
       - icon_name: NETWORK_DRIVE
       - SvRengaConnectNode
       - SvRengaCreateColumnsNode
       - SvRengaGetWallsNode
   ```

4. **Сохраните файл**

**Подробная инструкция:** см. файл `ДОБАВИТЬ_В_YAML.md`

### ШАГ 3: Перезапуск

1. **Перезапустите Blender** (полностью закрыть и открыть заново)
2. **Откройте Sverchok** (Shift+A в Node Editor)
3. **Найдите категорию "Renga"** в меню между "Network" и "Pulga Physics"

### Вариант 2: Ручная регистрация (если скрипт не работает)

Выполните в консоли Blender (Window > Toggle System Console):

```python
import bpy
import sys
import os
import importlib.util

# Путь к папке renga
renga_path = r"C:\Users\ВАШЕ_ИМЯ\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga"

# Регистрация каждой ноды
nodes = [
    ("renga_connect.py", "SvRengaConnectNode"),
    ("renga_create_columns.py", "SvRengaCreateColumnsNode"),
    ("renga_get_walls.py", "SvRengaGetWallsNode"),
]

for filename, class_name in nodes:
    filepath = os.path.join(renga_path, filename)
    module_name = filename.replace(".py", "")
    
    spec = importlib.util.spec_from_file_location(module_name, filepath)
    module = importlib.util.module_from_spec(spec)
    module.__package__ = "nodes.renga"
    spec.loader.exec_module(module)
    
    if hasattr(module, 'register'):
        try:
            if class_name in dir(bpy.types):
                bpy.utils.unregister_class(getattr(bpy.types, class_name))
            module.register()
            print(f"✓ {class_name} зарегистрирован")
        except Exception as e:
            print(f"✗ Ошибка регистрации {class_name}: {e}")

# Проверка
for _, class_name in nodes:
    print(f"{class_name}: {'✓' if class_name in dir(bpy.types) else '✗'}")
```

---

## 🔍 ПРОВЕРКА:

### Проверка 1: Ноды зарегистрированы?
```python
import bpy
print('SvRengaConnectNode' in dir(bpy.types))  # Должно быть True
print('SvRengaCreateColumnsNode' in dir(bpy.types))  # Должно быть True
print('SvRengaGetWallsNode' in dir(bpy.types))  # Должно быть True
```

### Проверка 2: Ноды имеют категорию?
```python
import bpy
print(bpy.types.SvRengaConnectNode.sv_category)  # Должно быть "Renga"
print(bpy.types.SvRengaCreateColumnsNode.sv_category)  # Должно быть "Renga"
print(bpy.types.SvRengaGetWallsNode.sv_category)  # Должно быть "Renga"
```

### Проверка 3: Ноды видны в меню?
1. Откройте Sverchok (Shift+A в Node Editor)
2. Найдите категорию "Renga" в меню
3. ИЛИ используйте поиск: Space -> введите "Renga"

---

## ⚠️ ЕСЛИ НЕ РАБОТАЕТ:

### Проблема 1: Ноды зарегистрированы, но не видны в меню
**Решение:**
1. Перезапустите Blender полностью
2. Перезагрузите аддон Sverchok:
   - Edit > Preferences > Add-ons
   - Найдите "Sverchok"
   - Снимите галочку (отключите)
   - Поставьте галочку обратно (включите)

### Проблема 2: Ошибка при регистрации
**Решение:**
1. Проверьте, что все файлы на месте
2. Проверьте, что в каждом файле есть `register()` и `unregister()`
3. Проверьте, что все ноды имеют `sv_category = "Renga"`
4. Удалите папку `__pycache__` в `nodes/renga/` (если есть)
5. Перезапустите Blender

### Проблема 3: Файлы не найдены
**Решение:**
1. Убедитесь, что папка `renga` находится в:
   ```
   C:\Users\ВАШЕ_ИМЯ\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
   ```
2. Проверьте путь в скрипте `FINAL_REGISTER_RENGA_NODES.py`
3. Обновите путь вручную, если нужно

---

## 📝 ЧЕКЛИСТ:

- [ ] Файлы скопированы в правильную папку
- [ ] `__init__.py` содержит `register()` и `unregister()`
- [ ] Все ноды имеют `sv_category = "Renga"`
- [ ] Все файлы нод имеют функции `register()` и `unregister()`
- [ ] Выполнен скрипт `FINAL_REGISTER_RENGA_NODES.py`
- [ ] Blender перезапущен
- [ ] Ноды видны в меню Sverchok

---

## 🎉 ПОСЛЕ УСПЕШНОЙ РЕГИСТРАЦИИ:

Ноды будут автоматически появляться в меню Sverchok при каждом запуске Blender, если:
1. ✅ Файлы находятся в правильной папке
2. ✅ `__init__.py` правильно настроен
3. ✅ Все ноды имеют `sv_category = "Renga"`

**Больше НЕ НУЖНО запускать скрипты вручную!**

---

## 📞 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ:

1. Проверьте версию Sverchok (должна быть 1.4.0 или новее)
2. Проверьте версию Blender (должна быть 4.2+ или 5.0+)
3. Проверьте консоль Blender на ошибки
4. Попробуйте использовать поиск в Sverchok: Space -> "Renga"

