# 🎯 ФИНАЛЬНАЯ УСТАНОВКА - ПОЛНОСТЬЮ АВТОМАТИЧЕСКАЯ

## ✅ Теперь всё автоматически!

Папка `sverchok_nodes\renga` теперь **полностью автоматическая**:

1. ✅ Ноды регистрируются автоматически (Sverchok)
2. ✅ Категория добавляется в меню автоматически (`__init__.py`)

## 🚀 Установка на ЛЮБОЙ Blender

### Просто скопируйте папку!

1. **Скопируйте папку `renga`** в:
   ```
   %APPDATA%\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
   ```
   
   Или для Blender 4.x:
   ```
   %APPDATA%\Blender Foundation\Blender\4.0\scripts\addons\sverchok-master\nodes\renga\
   ```

2. **Перезапустите Blender** - ВСЁ!

   При загрузке Sverchok:
   - ✅ Ноды автоматически зарегистрируются
   - ✅ Категория "Renga" автоматически добавится в меню

3. **Готово!** Откройте Sverchok → Add Node → категория "Renga"

## 📦 Что в папке renga

```
renga/
  ├── __init__.py              # Автоматическая регистрация нод и меню
  ├── renga_connect.py         # Нода подключения
  ├── renga_create_columns.py  # Нода создания колонн
  ├── renga_get_walls.py       # Нода получения стен
  ├── renga_client.py          # TCP клиент
  ├── commands.py              # Команды для Renga
  └── connection_protocol.py   # Протокол связи
```

## 🔧 Как это работает

1. **Sverchok** автоматически сканирует папку `nodes/renga/` и импортирует все `.py` файлы
2. **`__init__.py`** при импорте:
   - Импортирует модули нод (они регистрируются автоматически)
   - Проверяет наличие категории "Renga" в меню
   - Если нет - добавляет автоматически в оба файла меню

## ⚠️ Важно

- В папке `renga` должны быть **ТОЛЬКО** файлы нод
- Не копируйте скрипты установки (`ADD_TO_MENU.py`, `FORCE_MENU.py`) в папку `renga`
- Sverchok пытается импортировать все `.py` файлы как ноды!

## 🎉 Итог

**Просто скопируйте папку `renga` → Перезапустите Blender → Готово!**

Всё остальное происходит автоматически! 🚀

