# 🚀 АВТОМАТИЧЕСКАЯ УСТАНОВКА НОД RENGA

## Описание

Эта инструкция описывает **полностью автоматическую** установку нод Renga в Sverchok. Все происходит автоматически:
- ✅ Копирование файлов
- ✅ Добавление в меню Sverchok
- ✅ Регистрация нод

## Варианты установки

### Вариант 1: PowerShell скрипт (РЕКОМЕНДУЕТСЯ для Windows)

1. **Закройте Blender** (если открыт)

2. **Запустите скрипт** `INSTALL_AUTO.ps1`:
   - Правой кнопкой на файл → "Запустить с PowerShell"
   - Или откройте PowerShell и выполните:
     ```powershell
     cd "C:\Program Files\Renga Standard\RengaSDK\Samples\C#\Renga_Sverchok"
     .\INSTALL_AUTO.ps1
     ```

3. **Скрипт автоматически:**
   - Найдет версию Blender
   - Скопирует файлы в папку `renga`
   - Добавит секцию в меню Sverchok (YAML)
   - Покажет результаты

4. **Перезапустите Blender**

5. **Готово!** Ноды появятся в меню Sverchok в категории "Renga"

### Вариант 2: Python скрипт в Blender

1. **Откройте Blender**

2. **Откройте Text Editor** (Shift+F11 или вкладка Scripting)

3. **Откройте файл** `INSTALL_AUTO.py`:
   - Open → выберите `INSTALL_AUTO.py`

4. **Нажмите Run Script** (Alt+P или кнопка Run Script)

5. **Скрипт автоматически:**
   - Скопирует файлы в папку `renga`
   - Добавит секцию в меню Sverchok (YAML)
   - Зарегистрирует ноды

6. **Перезапустите Blender**

7. **Готово!** Ноды появятся в меню Sverchok

---

## Что делает скрипт

### Шаг 1: Копирование файлов
- Создает папку `renga` в `nodes\` Sverchok (если не существует)
- Копирует все файлы из `sverchok_nodes\renga\`:
  - `__init__.py`
  - `renga_connect.py`
  - `renga_create_columns.py`
  - `renga_get_walls.py`
  - `renga_client.py`
  - `commands.py`
  - `connection_protocol.py`

### Шаг 2: Добавление в меню
- Находит папку `menus\` Sverchok
- Открывает файлы меню:
  - `full_by_data_type.yaml`
  - `index.yaml`
  - `full_by_operations.yaml`
- Добавляет секцию "Renga" между "Network" и "Pulga Physics"
- Создает резервные копии файлов (*.backup)

### Шаг 3: Регистрация нод
- Регистрирует ноды в Blender (Python скрипт)
- Или ноды регистрируются автоматически при следующем запуске Blender

---

## Проверка установки

### После установки:

1. **Перезапустите Blender полностью**

2. **Откройте Sverchok** (Shift+A в Node Editor)

3. **Найдите категорию "Renga"** в меню между "Network" и "Pulga Physics"

4. **Должны быть видны три ноды:**
   - Renga Connect
   - Renga Create Columns
   - Renga Get Walls

### Если ноды не видны:

1. **Перезагрузите аддон Sverchok:**
   - Edit > Preferences > Add-ons
   - Найдите "Sverchok"
   - Снимите галочку (отключите)
   - Поставьте галочку обратно (включите)

2. **Используйте поиск:**
   - Space → введите "Renga"
   - Ноды должны появиться в результатах поиска

3. **Проверьте регистрацию:**
   - Откройте Text Editor
   - Выполните:
     ```python
     import bpy
     print('SvRengaConnectNode' in dir(bpy.types))
     print('SvRengaCreateColumnsNode' in dir(bpy.types))
     print('SvRengaGetWallsNode' in dir(bpy.types))
     ```
   - Все должны вернуть `True`

---

## Структура после установки

```
C:\Users\<ВашеИмя>\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\
├── nodes\
│   └── renga\
│       ├── __init__.py
│       ├── renga_connect.py
│       ├── renga_create_columns.py
│       ├── renga_get_walls.py
│       ├── renga_client.py
│       ├── commands.py
│       └── connection_protocol.py
└── menus\
    ├── full_by_data_type.yaml  ← содержит секцию Renga
    ├── index.yaml              ← содержит секцию Renga
    └── full_by_operations.yaml ← содержит секцию Renga
```

---

## Восстановление из резервных копий

Если что-то пошло не так, восстановите файлы меню из резервных копий:

1. Найдите файлы `*.backup` в папке `menus\`
2. Скопируйте их обратно (удалите расширение `.backup`)
3. Перезапустите Blender

---

## Требования

- **Blender** (версия 4.0+ или 5.0+)
- **Sverchok** - установленный и активированный аддон
- **Windows** (для PowerShell скрипта) или любой ОС (для Python скрипта)

---

## Известные проблемы

### Проблема: Скрипт не находит папку Sverchok

**Решение:**
- Убедитесь, что Sverchok установлен
- Проверьте версию Blender
- Укажите путь вручную в скрипте

### Проблема: Файлы меню не найдены

**Решение:**
- Это нормально, если Sverchok использует другую структуру меню
- Ноды все равно будут работать через поиск (Space → "Renga")

### Проблема: Ноды не появляются в меню

**Решение:**
- Перезапустите Blender полностью
- Перезагрузите аддон Sverchok
- Используйте поиск (Space → "Renga")

---

## Итог

После выполнения скрипта установки:
- ✅ Все файлы на месте
- ✅ Меню обновлено
- ✅ Ноды готовы к использованию

**Просто перезапустите Blender и начните использовать ноды!**

