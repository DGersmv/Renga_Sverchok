# КАК ЗАСТАВИТЬ НОДЫ ПОЯВИТЬСЯ В МЕНЮ SVERCHOK

## ✅ ВСЕ ФАЙЛЫ ГОТОВЫ В `sverchok_nodes/`

Все файлы сохранены в папке проекта. Скопируйте папку `sverchok_nodes` и переименуйте в `renga`, затем вставьте в Sverchok.

## 🚀 ШАГ 1: Копирование файлов

1. Скопируйте папку `sverchok_nodes` из проекта
2. Переименуйте её в `renga`
3. Вставьте в:
   ```
   C:\Users\[ВашеИмя]\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\sverchok-master\nodes\renga\
   ```
4. Удалите `__pycache__` в папке `renga` (если есть)

## 🔧 ШАГ 2: Принудительная регистрация в меню

### Вариант A: Через скрипт в Blender

1. Откройте Blender
2. Перейдите в **Text Editor**
3. Создайте новый текст (New)
4. Скопируйте содержимое файла `FORCE_MENU.py` из папки `sverchok_nodes`
5. Запустите скрипт (Run Script или Alt+P)
6. Проверьте вывод в консоли

### Вариант B: Перезагрузка аддона Sverchok

1. Откройте Blender
2. **Edit > Preferences > Add-ons**
3. Найдите "Sverchok"
4. Снимите галочку (отключите)
5. Поставьте галочку обратно (включите)
6. Перезапустите Blender полностью

## 🔍 ШАГ 3: Проверка регистрации

Выполните в консоли Blender (Text Editor > Run Script):

```python
import bpy
print("Проверка нод:")
print("SvRengaConnectNode:", 'SvRengaConnectNode' in dir(bpy.types))
print("SvRengaCreateColumnsNode:", 'SvRengaCreateColumnsNode' in dir(bpy.types))
print("SvRengaGetWallsNode:", 'SvRengaGetWallsNode' in dir(bpy.types))
```

Если все три команды вернут `True` - ноды зарегистрированы!

## 📋 ШАГ 4: Поиск нод в меню

### Если ноды зарегистрированы, но не видны в меню:

1. Откройте Sverchok в Blender
2. Нажмите **Space** (или кнопку поиска в меню)
3. Введите **"Renga"** или **"SvRenga"**
4. Ноды должны появиться в результатах поиска!

### Если ноды не видны даже в поиске:

Попробуйте добавить ноды вручную через Python:

```python
import bpy
from bpy.types import NodeTree

# Создать дерево нод Sverchok
tree = bpy.context.space_data.edit_tree
if tree and tree.bl_idname == 'SverchCustomTreeType':
    # Добавить ноду через API
    node = tree.nodes.new('SvRengaConnectNode')
    node.location = (0, 0)
    print("✓ Нода добавлена вручную!")
```

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Sverchok v1.4.0 + Blender 5.0

Есть известная проблема, когда ноды регистрируются, но не появляются в меню категорий. Это проблема Sverchok, а не наших нод.

**Решение:** Используйте поиск (Space) для доступа к нодам.

## 📝 ЧТО БЫЛО ИСПРАВЛЕНО

1. ✅ Добавлена явная регистрация в `__init__.py`
2. ✅ Добавлен `sv_icon` ко всем нодам
3. ✅ Исправлены импорты (убраны относительные импорты)
4. ✅ Создан скрипт `FORCE_MENU.py` для принудительной регистрации

## 🎯 ИТОГ

**Все файлы готовы в `sverchok_nodes/`!**

Если ноды регистрируются (проверка через `dir(bpy.types)` возвращает `True`), но не видны в меню - это проблема Sverchok с Blender 5.0. Используйте поиск (Space) для доступа к нодам.



