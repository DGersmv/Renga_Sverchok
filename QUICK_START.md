# Быстрый старт - Как использовать плагины

## ⚠️ ВАЖНО: Правильное подключение компонентов

### Шаг 1: Подготовка Renga
1. **Закройте Renga** (если запущен) - это нужно для сборки плагина
2. Пересоберите проект в Visual Studio: **Build → Rebuild Solution**
3. Запустите Renga
4. Нажмите кнопку **"RNG"** в интерфейсе Renga
5. Нажмите **"Start Server"** в открывшейся форме
6. Убедитесь, что статус показывает **"Running"** (зеленый)

### Шаг 2: Подготовка Grasshopper
1. Перезапустите Rhino/Grasshopper (чтобы загрузить обновленный плагин)
2. Компоненты должны появиться в категории **"RNG"**

### Шаг 3: Подключение компонентов в Grasshopper

#### Компонент "RNG" (главная нода):
1. Добавьте компонент **"RNG"** на канвас
2. Подключите:
   - **Port** (вход 0): номер порта, по умолчанию `50100`
   - **Connect** (вход 1): установите `True` для подключения
3. Проверьте выходы:
   - **Connected** (выход 0): должен показать `True` если подключен
   - **Message** (выход 1): должно быть "Connected to Renga on port 50100"
   - **Client** (выход 2): ⚠️ **ЭТОТ ВЫХОД НУЖНО ПОДКЛЮЧИТЬ К INPUT!**

#### Компонент "Input":
1. Добавьте компонент **"Input"** на канвас
2. Подключите:
   - **Points** (вход 0): список точек (Point3d) из Grasshopper
   - **RengaConnect** (вход 1): ⚠️ **ПОДКЛЮЧИТЕ ВЫХОД "Client" (3-й выход) ИЗ КОМПОНЕНТА "RNG"!**
3. Правый клик по компоненту **"Input"** → выберите **"Update - Send to Renga"**
4. Колонны должны появиться в Renga!

## ❌ Частые ошибки:

### Ошибка: "Failed to extract client. Received type: GH_Boolean"
**Причина:** Подключен не тот выход из компонента "RNG"
**Решение:** 
- Отключите текущее соединение
- Подключите **выход "Client"** (3-й выход, не "Connected"!)

### Ошибка: "Renga Connect is not connected"
**Причина:** Компонент "RNG" не подключен к серверу
**Решение:**
1. Убедитесь, что в компоненте "RNG" параметр **"Connect" = True**
2. Убедитесь, что сервер запущен в Renga (кнопка "RNG" → "Start Server")
3. Проверьте, что порт совпадает (50100 по умолчанию)

### Ошибка: "Click Update button to send points"
**Причина:** Не нажата кнопка Update
**Решение:** Правый клик по компоненту "Input" → "Update - Send to Renga"

## 📋 Правильная схема подключения:

```
[Point List] → [Input: Points]
                    ↓
[RNG Component]    [Input Component]
  Outputs:            Inputs:
  0. Connected        0. Points ← точки
  1. Message          1. RengaConnect ← Client из RNG!
  2. Client ✅────────┘
```

## 🔧 Если ничего не работает:

1. **Закройте Renga и Grasshopper**
2. **Пересоберите проект:** Build → Rebuild Solution
3. **Перезапустите Renga** → запустите сервер (кнопка "RNG" → "Start Server")
4. **Перезапустите Rhino/Grasshopper**
5. **Проверьте подключение:**
   - RNG: Connect = True
   - RNG → Client → Input: RengaConnect
   - Input: правый клик → "Update - Send to Renga"

